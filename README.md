# 📝 Tasks API — FastAPI + MongoDB + Docker

CRUD de **Tarefas** construído com FastAPI, pymongo e Docker, com base em Arquitetura Modular:

---

## 🚀 Como executar

```bash
docker-compose up --build
```
